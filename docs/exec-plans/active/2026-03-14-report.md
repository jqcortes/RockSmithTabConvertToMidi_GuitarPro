# 2026-03-14 進捗レポート

## 概要
- 目的: `CRASH COMPLEXION` の 11 ページ変換を完走させるため、Audiveris STEMS 失敗の原因を特定する。
- 結果: 直接原因候補を絞り込み、回避パラメータ候補の挙動を検証した。

## 今日の成果
- `page_011` の失敗事象を再現し、`book.xml` の `steps` に `STEMS` が記録されないことを確認した。
- `StemsRetriever.inspectStems` 周辺を逆引きし、`BeamLinker.inspectVLinkers()` -> `StemBuilder` の経路を特定した。
- `StemBuilder.retrieveLengths()` の `lengthMap` 未設定パターンを把握し、`gapHigh_p4` 調整時の挙動を整理した。
- `gapHigh_p4=0.01` と `gapHigh_p4=1.9` の比較を行い、どちらでも最終的に `Error processing stub` が残ることを確認した。
- 多数発生する `SLF4J(E): Failed toString() invocation on StemBuilder` は、`logger.debug("{}", this)` による副次的エラーであり、主因ではない可能性が高いと判断した。

## 現在の課題
- `Book: Error processing stub` の直接要因が未特定。
- `STEMS` 完了前提の `PAGE -> EXPORT` が未達。
- 全 11 ページの最終 MIDI 生成は継続中。

## 展望 (次アクション)
- `SheetStub.reachStep()` / `doOneStep()` と `Book` 側の失敗判定フローを確認し、例外ログが出ない失敗経路を特定する。
- `gapHigh_p4=1.9` を基本線に、`-step PAGE -export` 直実行で MXL 生成可否を再検証する。
- 生成成功ページから先に MIDI 連結を進める暫定運用を検討し、上流バグ調査を並行トラック化する。

## 判断メモ
- 上流 (Audiveris) の根修正は長期化しやすいため、短期は「完走率優先の回避策」、中期で「最小再現 + upstream 報告」の二段構えを採用する。